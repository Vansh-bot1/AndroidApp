<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.freshy">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:label="FreSHy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".news_main"
            android:label="News" />
        <activity
            android:name=".markets_main"
            android:label="Markets">
        </activity>
        <activity
            android:name=".others_main"
            android:label="Others" />
        <activity
            android:name=".marketview"
            android:label="Latest" />
        <activity android:name=".othersview"
            android:label="Others"/>
    </application>

</manifest>